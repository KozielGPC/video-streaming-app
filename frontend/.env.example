# API Configuration
# Base URL for the backend API (video metadata service - not implemented yet)
VITE_API_URL=http://localhost:8080/api

# Livestream Configuration
# URL for the HLS playback service (go-app-livestream playback server)
# This serves HLS streams at: {url}/live/{streamName}/index.m3u8
VITE_LIVESTREAM_URL=http://localhost:8001

# WebSocket Configuration
# URL for the chat WebSocket service (chat-service with Socket.io)
# Uses 'chat:message' event for sending/receiving messages
VITE_CHAT_WS_URL=http://localhost:3000

# ============================================
# Backend Services Reference
# ============================================
# 
# go-app-livestream:
#   - Auth Server (Port 8000): POST /auth - Authenticates streaming keys
#   - Playback Server (Port 8001): GET /live/:name/* - Serves HLS streams
#   - RTMP Server (Port 1935): For OBS/streaming software to push streams
#   - Database: PostgreSQL (Port 5432) - Stores stream keys
#
# chat-service:
#   - WebSocket (Port 3000): Socket.io for real-time chat
#   - Events: 'chat:message' (send/receive), 'connect', 'disconnect'
#   - Redis (Port 6379): Pub/sub for scaling
#   - MongoDB (Port 27017): Chat log storage
#
# To test livestreaming:
#   1. Start go-app-livestream: docker-compose up
#   2. Start chat-service: docker-compose up
#   3. Push a stream via OBS to rtmp://localhost:1935/live/{streamKey}
#   4. View stream in frontend at /live/{streamId}
